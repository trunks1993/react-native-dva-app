import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    content: {
        paddingLeft: 240,
        paddingRight: 240,
        justifyContent: 'space-between',
        alignItems: 'center',
        display: 'flex',
        flexDirection: 'row',
        marginTop: 180,
    }
})
